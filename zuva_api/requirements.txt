fastapi==0.104.1
uvicorn[standard]==0.24.0
python-telegram-bot==20.7
pydantic==2.5.0

# zoneinfo needs a tz database, which slim images do not ship. Without this
# quiet hours are evaluated in UTC instead of TIMEZONE.
tzdata==2023.4
